{
  "name": "@proofgrid/core",
  "version": "0.2.0-draft.0",
  "private": true,
  "description": "Local evidence assessment CLI and SDK with source-backed external rules",
  "license": "MPL-2.0",
  "type": "module",
  "files": [
    "schemas/0.1/*.json",
    "docs/contracts.md",
    "docs/standards-mapping.md",
    "LICENSING.md",
    "LICENSES/Apache-2.0.txt",
    "dist/src/*",
    "dist/schemas/0.1/*.json"
  ],
  "exports": {
    "./schemas/0.1/evidence.schema.json": "./schemas/0.1/evidence.schema.json",
    "./schemas/0.1/rule-pack.schema.json": "./schemas/0.1/rule-pack.schema.json",
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/proofgrid-energy/proofgrid-core.git"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "npm run build && node --test --test-isolation=none test/*.test.mjs",
    "test:package": "npm run build && node scripts/prepare-package.mjs && npm pack --ignore-scripts --pack-destination .local-checks/package && npm install --prefix .local-checks/consumer --ignore-scripts --package-lock=false ./.local-checks/package/proofgrid-core-0.2.0-draft.0.tgz && node scripts/check-package-consumer.mjs .local-checks/consumer && tsc --ignoreConfig --noEmit --module NodeNext --target ES2022 .local-checks/consumer/smoke.ts"
  },
  "bin": {
    "proofgrid": "./dist/src/cli.js"
  }
}
